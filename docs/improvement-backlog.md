# Improvement Backlog

Durable memory of the agent improvement loop ([operating doc](agent-improvement-loop.md), [ADR 0027](adr/0027-agent-improvement-loop.md)). Format and status lifecycle: `.claude/skills/improvement-cycle/references/backlog-format.md`.

Lifecycle: `proposed → accepted → in-pr (#NNN) → merged → verified` | `rejected`. IDs never reused. Dedupe before adding.

| ID | Item | Source | Severity | Tier | Status | Updated | Evidence / notes |
|---|---|---|---|---|---|---|---|
| BL-001 | `metadata.ts` claimed wave-launcher is "gated by approvals"; code has no gate (`wave-launcher/index.ts:8,220`) | seed audit | P2 | T1 | in-pr (bootstrap) | 2026-07-07 | Fixed in bootstrap PR — description now matches code |
| BL-002 | `.env.example` claimed agents use `@anthropic-ai/claude-agent-sdk`; they use plain `@anthropic-ai/sdk` | seed audit | P3 | T0 | in-pr (bootstrap) | 2026-07-07 | Fixed in bootstrap PR |
| BL-003 | `next-engineer.md` + `leadlandlord-architect.md` referenced a machine-local `~/.claude/plans/...` file that doesn't exist in cloud sessions | seed audit | P2 | T1 | in-pr (bootstrap) | 2026-07-07 | Fixed in bootstrap PR — now reference DEPRECATIONS.md + README phase table |
| BL-004 | DEPRECATIONS.md marks closer-agent / billing-dunning / churn-recovery / compliance-guard "pending, out of MVP scope", yet all four are registered and three are cron-scheduled (`apps/operator/vercel.json:9-11`). Decide: pause the schedules, or update the deprecation status | seed audit | P1 | T3 | in-pr (#264) | 2026-07-20 | compliance-guard confirmed live `ON(2)` (disposition.ts:63) but DEPRECATIONS.md labeled it deferred; closer/billing/churn confirmed OFF and correctly gated (disposition.ts:85-87) — relabeled built+gated-off. molly-digest documented (see BL-025). Runtime gating is correct (OFF agents keep crons deliberately); only the doc was stale (no behavior/gate change). Per the Phase-6-agent guardrail (SKILL.md), ships as a decision PR surfacing to Mike: confirm compliance-guard being live `ON(2)` is intended, and the roadmap disposition (keep-gated vs delete) of the closer/billing/churn trio. |
| BL-005 | citation-runner TODOs: move directory list to a Sanity doc field (`citation-runner` scheduler L17/L42) | seed audit | P3 | T1→code | proposed | 2026-07-07 | |
| BL-006 | network-linker TODO: needs typed Sanity page helper (`network-linker/index.ts:286`) | seed audit | P3 | code | proposed | 2026-07-07 | |
| BL-007 | tenant-prospector Phase-7 TODO: website scraping + Hunter.io fallback (`tenant-prospector/index.ts:40`) | seed audit | P3 | code | proposed | 2026-07-07 | Phase-gated; confirm phase before building |
| BL-010 | call-classifier active learning: harvest sub-0.5-confidence hand-review outcomes from DB into few-shot examples in the classifier prompt each cycle | gap analysis | P2 | T1 | proposed | 2026-07-07 | High value / low risk — compounding accuracy; ideal first cycle target |
| BL-011 | molly-inbox manual_review hardening: cluster manual_review fallbacks by cause, extend reply-state-machine prompt coverage (ADR 0006) | gap analysis | P2 | T1 | proposed | 2026-07-07 | High value / low risk — ideal first cycle target |
| BL-012 | citation-packet-preparer: after BL-005, generate prefilled per-directory submission packets + auto-verify pasted listing URLs; human/VA still clicks submit | gap analysis | P2 | code | proposed | 2026-07-07 | Cuts VA hours; full form automation deliberately out of scope |
| BL-013 | Molly draft quality-gated auto-approve: molly-scorer score ≥ threshold + daily send cap auto-approves guest-post drafts; below → human queue | gap analysis | P2 | T3 | proposed | 2026-07-07 | GATE CHANGE decision PR; rollback = threshold flag |
| BL-014 | Medium-risk SEO auto-apply with rollback journal: journal prior state, auto-revert on GSC regression within 14d; high-risk stays blocked | gap analysis | P2 | T3 | proposed | 2026-07-07 | Needs rollback infra first; GATE CHANGE decision PR |
| BL-015 | Content-idea category-scoped auto-approve (e.g. seasonal refresh) with weekly cap | gap analysis | P3 | T3 | proposed | 2026-07-07 | GATE CHANGE decision PR |
| BL-016 | Self-healing retry runbooks for manual-fix fallbacks: domain DNS/attach failures (`domain-procurer` L225/245/260), trial cleanup, tracking-number assignment — bounded auto-retry with backoff before emitting manual_fix | gap analysis | P2 | code | proposed | 2026-07-07 | One PR per fallback class |
| BL-017 | Digest→event bridge: fleet-digest / portfolio-analyst items with known dispositions emit `agent_events` instead of prose-only email | gap analysis | P3 | code | proposed | 2026-07-07 | |
| BL-018 | Content-migration "trivial class" auto-apply (pure metadata moves) | gap analysis | P3 | T3 | proposed | 2026-07-07 | Deferred — low value relative to risk |
| BL-019 | Go-live checklist *verification* automation: agent verifies every checklist item and reports; human keeps the promote-to-live click | gap analysis | P3 | code | proposed | 2026-07-07 | Never automate the click itself |
| BL-020 | Domain approval batch UX: keep the human gate (spends money), improve batch-approval ergonomics in `/operator` | gap analysis | P3 | code | proposed | 2026-07-07 | UX-only |
| BL-021 | Automated cross-site internal-link-pattern similarity check: extend footprint review to compare link caps/anchors/targets across the network now that internal-linker covers faq/info/service-area pages | seo audit 2026-07 (Phase 3) | P2 | code | merged (#259) | 2026-07-13 | Confirmed merged to `main` via #259 (`64b33cf`, 2026-07-12): `packages/agents/src/audit/content-similarity.ts` + `content-footprint.ts` exist with tests (`__tests__/content-similarity.test.ts`), wired into `apps/operator/app/operator/networks/[id]/page.tsx`. → `verified` once someone confirms the operator page renders against a live network (repo-only mode can't). Thresholds: FAQ overlap >40% of smaller set, link-signature match >80% of shared page kinds. |
| BL-022 | Operator agents page renders a blank description for 13 of 47 registered agents because they had no `agentMetadata` entry (`metadata.ts` 34 vs `registry.ts` 47); consumer `apps/operator/app/operator/agents/page.tsx:138` uses `meta?.description ?? ''` | cycle 2026-07-13 | P2 | T1 | merged (#261) | 2026-07-13 | Added 13 metadata entries (descriptions authored from each agent's source; `trigger` only for non-cron agents) + `__tests__/registry-metadata-parity.test.ts` guard. QA: `pnpm --filter @leadlandlord/agents test` 1579 pass, `tsc --noEmit` clean |
| BL-023 | `scripts/seed-agent-schedules.ts` cron cadences drifted from authoritative `apps/operator/vercel.json` for maintenance / portfolio-analyst / network-metrics-aggregator (over-run) and geo-aeo-auditor (under-run — re-encoded the Mon-only bug #259 fixed in vercel.json). DB tick (`poll-tick.ts`) treats agent_schedules as source of truth | cycle 2026-07-13 (fleet-performance-analyst) | P2 | T2 | merged (#262) | 2026-07-13 | Architect APPROVE (vercel.json authoritative; ~$0 impact — none of the 4 make paid Anthropic/DataForSEO calls; net reduces run volume + fixes geo-aeo coverage). Fixed 4 cronExpr literals + docstring. |
| BL-024 | `buildsell-review-refresh` is in `apps/operator/vercel.json:36` (`0 8 1 * *`) but has NO row in `scripts/seed-agent-schedules.ts` SEED — the seed's own honesty check (L88-91) warns on it. Not a live problem today (vercel.json direct route still fires it), but becomes a silent permanent outage the day vercel.json's per-agent cron lines are trimmed (the `poll-tick.ts:16-17` fast-follow) | cycle 2026-07-13 (leadlandlord-architect) | P2 | T2 | proposed | 2026-07-21 | Add a `buildsell-review-refresh` SeedRow (`cadenceKind:'cron', cronExpr:'0 8 1 * *'`). Blocker for the eventual vercel.json cron-trim; kept out of BL-023 to keep that diff tight. Reconfirmed open 2026-07-21 (triage) — no drift. |
| BL-025 | molly-digest fully decommission: OFF('superseded by fleet-digest') yet still fires daily via BOTH apps/operator/vercel.json:21 cron AND scripts/seed-agent-schedules.ts:46 seed row → wasted daily scheduler invocation (runScheduler builds candidates then run-scheduler.ts drops all as disabled, enqueued:0; no paid API spend). Remove both the vercel.json line and the seed row + add DEPRECATIONS row (row added this cycle). | cycle 2026-07-20 | P3 | T2 | proposed | 2026-07-21 | #262 has since landed — no longer blocked; ready for next-engineer to remove the vercel.json line + seed row. Reconfirmed open 2026-07-21 (triage) — no drift. |
| BL-026 | audit/content-footprint.ts (shipped in #259/BL-021) has no test file unlike sibling pure modules content-similarity.ts + cross-link-footprint.ts; its IO loader + aggregation (faq→site mapping, businessName threading, try/catch-swallow) is unverified. | cycle 2026-07-20 | P3 | T1 | proposed | 2026-07-20 | next-engineer. May be intentional convention (IO untested, pure math tested) — confirm before writing. |
| BL-027 | `scripts/fleet-metrics.ts` crashes with exit 1 (`relation "agent_runs" does not exist`) instead of degrading to repo-only mode when `DATABASE_URL` is reachable but the agent schema is absent (unmigrated DB). The documented graceful path (`NO_DB`, exit 2) only fires when the URL is unset, so any reachable-but-unmigrated environment silently defeats DB-based triage. | triage 2026-07-21 (fleet-performance-analyst) | P2 | T1→code | proposed | 2026-07-25 | Catch the Postgres undefined-table error (42P01) and fall back to the same repo-only path as NO_DB. Surfaced when today's 24h + 7d metrics both errored against a Neon DB lacking `agent_runs`. Tooling-only; NOT touched during triage (fleet-metrics is the triage tool itself) — defer the fix to the weekly cycle. Reconfirmed 2026-07-22 (triage) — identical error string (`relation "agent_runs" does not exist`, exit 1) on both `--window 24h` and `--window 7d`; DB telemetry again unavailable this cycle. Reconfirmed 2026-07-23 (triage) — same error on both windows (exit 1); three consecutive days now with DB-based triage blind — increasingly worth prioritizing in the weekly cycle. Reconfirmed 2026-07-25 (triage) — same error on both windows (exit 1) after a fresh `pnpm install`; **fourth** triage in a row with DB-based signals (dead letters, budget pressure, failure-rate/dedupe/stale-schedule) completely blind. Strongly recommend this be the top item in the next weekly cycle. |
| BL-028 | `call-classifier` has zero test coverage (no `*.test.ts` references it) despite sitting in the ADR 0031 inbound-call chain that computes per-call revenue estimates; sibling `lead-qualifier` has `index.test.ts`. | triage 2026-07-21 (fleet-performance-analyst) | P3 | T1 | proposed | 2026-07-22 | `packages/agents/src/call-classifier/index.ts` (159 lines, no sibling test). Same "module untested" pattern as BL-026. next-engineer; add classification unit tests. Reconfirmed 2026-07-22 (triage) — still no `*.test.ts` references `call-classifier`. |
| BL-029 | `.claude/agents/fleet-performance-analyst.md:10` hardcodes the fleet size as "~42 agents in `packages/agents/src`", but the live registry now has 47 (`packages/agents/src/registry.ts`, parity-confirmed with `metadata.ts`). The persona-doc stat drifted as the fleet grew (SEO Phase 2-4 #257–#259 + BL-022 metadata backfill) after the doc was last written (2026-07-06). | triage 2026-07-22 (fleet-performance-analyst) | P3 | T1 | proposed | 2026-07-22 | Prompt-hygiene: a fact that lives elsewhere (the registry) drifted in a persona doc. agent-prompt-engineer; either bump "~42"→"47" or replace the literal with a "see registry.ts" pointer so it can't drift again. Cosmetic — the doc is a Claude Code subagent persona, not runtime fleet behavior. |
| BL-030 | Stale/misleading TODO comment in `packages/agents/src/scheduler/molly-nudge.ts:65` (`// TODO(sprint-3): replaced by network-linker / backlink-copycat / citation-runner`) sits mid-function above a live `events.push({ agent: 'molly', ... })` dispatch. The "replaced by" work appears already done (dispatch targets `molly` directly), so the marker is a leftover that misleads readers into thinking the block is dead. | triage 2026-07-23 (fleet-performance-analyst) | P3 | T1 | proposed | 2026-07-23 | Doc-hygiene only, zero functional risk — same class as already-tracked BL-005/006/007 TODOs. Confirm the sprint-3 replacement really landed, then delete or correct the one comment line. next-engineer (1-line). Not fixed in triage (triage may only touch the backlog on an unmerged prior branch). |
| BL-031 | Unverified-in-production TODO in `packages/agents/src/molly/nudge.ts:188` (`// TODO(Phase 4): verify Zoho reply threading is working live`) — an open operational question about whether `ZohoMail_sendReplyEmail` actually threads nudge replies in the recipient's inbox, with a documented `Re:`-subject fallback. Never confirmed against live Zoho behavior post-launch. | triage 2026-07-23 (fleet-performance-analyst) | P3 | code | proposed | 2026-07-23 | Operational verification, not a code defect — needs a live send + inbox check, not a repo change. Track so it isn't perpetually re-discovered. Owner: whoever runs the next Molly live send; downgrade/close once threading is confirmed. |
| BL-032 | Deferred-scope TODO in `packages/agents/src/content-engine/density-lint.ts:320` (`// TODO Sprint 2: add opts.city and check explicitly`) — the city-name over-density warn rule uses a token-frequency proxy heuristic (any 1-word >4-char token appearing 5+ times at ≥1% density) instead of an explicit `opts.city` field, so it can both false-positive on non-city tokens and miss the actual city. | triage 2026-07-23 (fleet-performance-analyst) | P3 | code | proposed | 2026-07-23 | Known scope-cut from an earlier sprint, never revisited; low value relative to a rewrite of the lint rule + all callers. Thread `opts.city` from the content-engine pipeline and check explicitly. next-engineer; confirm the accuracy gain is worth the caller churn before building. |
| BL-033 | Three registered, cron-scheduled agents are `disposition` `OFF` yet absent from `DEPRECATIONS.md`: `tenant-prospector` (`disposition.ts:82` `OFF('until tenants; Apollo/Places creds')`, cron `apps/operator/vercel.json:7` `0 14 * * 1`), `outreach-agent` (`disposition.ts:83` `OFF('until tenants; sends real email')`, cron `vercel.json:8` `0 15 * * *`), and `trial-manager` (`disposition.ts:84` `OFF('until trials exist')`, cron `vercel.json:9` `0 16 * * *`). Their Phase-6-outbound peers `billing-dunning`/`churn-recovery`/`closer-agent` (BL-004) and `molly-digest` (BL-025) all got `DEPRECATIONS.md` rows; these three were missed. | triage 2026-07-25 (fleet-performance-analyst) | P2 | T1 | proposed | 2026-07-25 | Same doc/decision-surfacing gap class as BL-004. Not a runtime or spend bug — OFF agents drop to `enqueued:0` at `run-scheduler.ts` with no paid API calls (established BL-025 pattern), and their crons still fire harmlessly. Doc-only fix: add three `DEPRECATIONS.md` rows (Gated off, Phase 6/tenant-pipeline, 2026-07-25) so the OFF+cron roster is complete and auditable — mirrors what BL-004 did for the other four. agent-prompt-engineer. NOT fixed in triage (triage may only touch the backlog on the unmerged prior branch). |
| BL-027 | Custom Sites line has no freshness/staleness loop — ADR 0033 D9 deferred it outright because a GROQ staleness report over `csPage`/`csPracticeArea`/`csPublication` would have nowhere to route findings (no `seoRecommendations` write, no operator UI, per D4/D8) | ADR 0033 Amendment 1 (D9) | P3 | code | proposed | 2026-07-25 | Revisit when either (a) the line hits ≥3 live custom sites, at which point it's a cheap additive check inside D8's audit script rather than a separate one, or (b) a Custom Sites operator UI exists for other reasons, giving staleness findings a manual-worklist surface. Do not build before a trigger fires — a report nobody consumes repeats the removed `agentApprovals` mistake. |
| BL-028 | Custom Sites IndexNow submission (`scripts/customsites-indexnow.ts`, ADR 0033 D7) is manual/operator-run only — no Vercel Cron trigger, since there's no `agent_events` row to hang one on for this DB-decoupled line | ADR 0033 Amendment 1 (D7) | P3 | code | proposed | 2026-07-25 | Revisit at ≥3 live custom sites: add a Vercel Cron route that re-runs the same script's logic on a schedule. Cron would be a clock only — still no Postgres row, no new agent, no `agent_events` coupling (D4 holds through the revisit). |
| BL-034 | `domain-procurer` + `tracking-setup` — the only two ARMED agents that fire irreversible real-money side effects (Namecheap domain registration; Twilio number provisioning ~$1/mo) — had zero test files, so a regression shipped straight to a live purchase/provision call with no safety net | cycle 2026-07-27 (fleet-performance-analyst F1) | P2 | T1 | in-pr (#272) | 2026-07-27 | Added Vitest coverage for both (`index.test.ts`), all integrations mocked (Namecheap/Vercel/Sanity/DB/Twilio → zero real calls); covers search/register routing, the operator-approval money guard (`registerDomain` not-called unless approved), dedupe-key correctness, hostKey sanitization, provision arg-forwarding. QA PASS. |
| BL-035 | Three currently-ON, production-scheduled agents have zero test coverage: `citation-runner`, `molly-copywriter` (+ untested `seo-expert/anchor-policy.ts` anchor-safety helper), `molly-inbox`/`classifier.ts` (reply-state-machine — BL-011 flags hardening but there's no harness under it) | cycle 2026-07-27 (fleet-performance-analyst F2) | P2 | T1 | proposed | 2026-07-27 | next-engineer. Follow-up to BL-034, same coverage-gap class on live agents. If done alongside BL-011, bundle the molly-inbox harness with the hardening. |
| BL-036 | `wave-launcher` is `OFF('skips backlinks; revisit later')` in `disposition.ts:93` but had no `DEPRECATIONS.md` row, unlike the sibling gated-off trio (closer/billing/churn) BL-004 reconciled | cycle 2026-07-27 (fleet-performance-analyst F3) | P3 | T1 | in-pr (#273) | 2026-07-27 | Adds a "Gated off" DEPRECATIONS row so the doc stays a trustworthy single source of truth for OFF agents. |
| BL-037 | `call-classifier` (`index.ts:89`) and `lead-qualifier` (`index.ts:115`) pin the dated Haiku snapshot `claude-haiku-4-5-20251001` while the rest of the fleet rides the floating alias `claude-haiku-4-5` — silent-breakage risk if the snapshot is retired. Not a billing bug (`estimateCostUsd` prefix-matches). | cycle 2026-07-27 (fleet-performance-analyst F4) | P3 | T1 | proposed | 2026-07-27 | Needs Mike's intent confirmation: was the pin deliberate (deterministic classifier)? If yes → add a clarifying comment; if no → align to the alias. Do NOT flip the pin unprompted (behavior-affecting). |
| BL-038 | `scripts/seed-fleet-disposition.ts:20-22` KNOWN_EXTRAS comment is stale (lists `citation-runner` as a later-phase extra/deferred stub though it's now a registered ON agent); `seed-agent-schedules.ts:80-81` citation-runner cron has no vercel.json line and no clarifying "DB-driven" comment like fleet-digest | cycle 2026-07-27 (fleet-performance-analyst F5) | P3 | T2 | proposed | 2026-07-27 | Comment-only doc hygiene, but touches seed scripts → T2, needs architect sign-off before editing. Functionally fine today (`poll-tick.ts` reads `agent_schedules` directly). |

Out of scope permanently: automating the niche approval gate (`/operator/niches`) — see ADR 0027.
